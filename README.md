https://karthii123-wq.github.io/html-tutorial/

HTML (HyperText Markup Language) is the basic building block of web development used to create and structure web pages.
It uses tags to define elements like headings, paragraphs, images, links, and forms. HTML works together with CSS for styling 
and JavaScript for functionality to build complete websites. It is easy to learn and widely used for designing both simple and complex web applications.
